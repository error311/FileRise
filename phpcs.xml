<?xml version="1.0"?>
<ruleset name="FileRise PSR-12 (PSR-4 only)">
    <description>PSR-12 for the PSR-4 backend tree only (relaxed for legacy side effects and long lines).</description>
    <rule ref="PSR12">
        <exclude name="PSR1.Files.SideEffects.FoundWithSymbols"/>
        <exclude name="Generic.Files.LineLength.TooLong"/>
    </rule>
    <file>src/FileRise</file>
</ruleset>
